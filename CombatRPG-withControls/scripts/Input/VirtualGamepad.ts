namespace CombatRPG {
    export namespace Input {
        export interface VirtualGamepad {
            right: Phaser.Button;
            left: Phaser.Button;
            up: Phaser.Button;
            down: Phaser.Button;
            action: Phaser.Button;
        }
    }
}