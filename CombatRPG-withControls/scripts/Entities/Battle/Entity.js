namespace CombatRPG {
    export namespace Entities {
        export namespace Battle {
            export class Entity {
                function testState()
                {
                    isAlive : boolean(false);
                    isMoving : Boolean(false);
                    
                    if (isAlive != false)
                    {
                        isMoving = true;
                    }
                }
            }
        }
    }
}